# MetaBlooms Coordination Events

Append-only event packets go here. Events are first-order state for multi-chat recovery.

Do not rewrite event history. Compact derived state into `recovery/*.json` only after validating events.
