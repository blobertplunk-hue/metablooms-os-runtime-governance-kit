# Evals Trace Review and Validator Alignment — Stage 1

Installed: minimum viable eval loop, trace-review schema, validator-alignment policy, seed traces, validator, and `mb evals --check --json`. Stage 2 must expand to 20-50 reviewed traces and scorecards before claiming mature eval coverage.
