# Pinned Evidence Receipt Promotion

Stage 14 promotes browser-local evidence pins into a SHA-bound receipt under `runtime/receipts/pinned_evidence/` and binds that receipt into the final authority export.
