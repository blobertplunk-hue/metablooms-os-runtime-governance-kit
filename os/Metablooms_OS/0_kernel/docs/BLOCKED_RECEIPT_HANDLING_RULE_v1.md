# BLOCKED_RECEIPT_HANDLING_RULE_v1

Blocked receipts go under `0_kernel/registry/blockers/` and are committed immediately when Git is usable. Blocked and repair entries do not advance `latest_pass`. Broad `git clean -fdx` is forbidden. Nested Git repos inside evidence directories are forbidden.

## Next Correct Command

`EXECUTE F7 — STABLE EXPORT + BOOT RECOVERY PROOF`
